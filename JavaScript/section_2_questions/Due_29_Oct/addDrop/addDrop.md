# You are given two files containing lists of student who registered for class, "before" and "after"

Your job is to:

- figure out which students dropped between the "before" and "after" classes and write them to the "drops" file.
- list the students who added between the classes in the "adds" file.
