# A Gentle Introduction to HTML

## Aspects

- HTML is a markup language
- HTML is a language that is used to create web pages
- HTML is a language that is used to create web pages that are displayed in a web browser
- HTML is a language that is used to create web pages that are displayed in a web browser that is used to display web pages

## Example

A paragraph is set of by a newline.
This is part of the paragraph.
And so is this.

```bash
echo "Hello, world!"
```

I use the search engine [Google](https://www.google.com) to find information.
